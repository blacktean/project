package com.bankTransfer.util;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import com.bankTransfer.service.IBaseService;

@Component
public class CheckIdentity {

	/*
	 * @Autowired private IBaseService baseService;
	 * 
	 * public static boolean checkIdentity() { Integer id =
	 * UserContext.getCurrent().getId(); if(baseService.checkUser(id)!= null) {
	 * return true; } return false; }
	 */

}
